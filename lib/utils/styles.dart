import 'package:flutter/painting.dart';

import 'size_config.dart';

class Styles {
  static final EdgeInsets marginJoystick = EdgeInsets.only(
    left: SizeConfig.blockSizeHorizontal * 5,
    bottom: SizeConfig.blockSizeVertical * 5,
  );
}
